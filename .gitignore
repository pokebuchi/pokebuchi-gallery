# GitHub に上げないもの
#
# 公開されるのは docs フォルダの中身だけです。
# 写真の原本や作業用のファイルは、このパソコンの中だけに置いておきます。

# 写真の原本（240MB以上あります。公開されるのは縮小した WebP だけ）
写真/

# ドロップしてまだ作品に割り当てていない写真
_取り込み中/

# 使い終わった取り込み用のファイル
使い終わったもの/

# ロゴの元データ（加工済みのものが docs に入っています）
ロゴ_赤アップ_背景透化.png

# 変換済みかどうかを覚えておくための記録
.build_cache.json

# Python が自動で作るもの
__pycache__/
*.pyc

# Windows / Mac が勝手に作るもの
Thumbs.db
desktop.ini
.DS_Store

# 確認用に作った見本
アイコンの見本.png
